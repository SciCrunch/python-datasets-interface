from setuptools import setup
setup(
    name = 'scicrunch',
    version = '0.1',
    packages = ['scicrunch'],
    author_email = '@ucsd.edu',
    url = 'githubrepo',
    install_requires = ['requests']
)
