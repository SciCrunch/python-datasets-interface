import sys, requests
sys.path.append('/home/kate/Code/src/')

from scicrunch import datasets

"""
datasetid = 42
lab name = ODC-SCI Demo Laboratory
getDataset
getInfo
getdata
"""

api_key = 'fnZNAi5gy5B0w83kJNGZrKOfX9xqhYpy'
dataset_id = '42'
dataset_name = 'Example Analysis Dataset'
lab_name = 'ODC-SCI Demo Laboratory'

Interface = datasets.Interface(api_key, lab_name)

# get data
data = Interface.getdata(dataset_name)
print(data)

# get fields
info = Interface.getInfo(dataset_name)
print(info)

headers = []
for i in info['fields']:
    headers.append(i)

print(headers)


matrix = [[0 for x in range(len(headers))]for y in range(len(data))]

for d in range(len(data)):
    for h in range(len(headers)):
        matrix[d][h] = d[h]

print(matrix)

